version: '3'
services:
  api:
    container_name: card-profile-go-api
    restart: always
    environment:
      - DOCKER_ENV = true
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - ${PORT}:${PORT}
    volumes:
      - .:/backend
